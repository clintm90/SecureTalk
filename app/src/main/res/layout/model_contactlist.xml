<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:tag="@string/app_name"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="5dp"
    android:paddingLeft="5dp">

    <ImageView
        android:id="@+id/model_contactList_icon"
        android:src="@drawable/ic_photo_none"
        android:background="@android:color/darker_gray"
        android:scaleType="centerCrop"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="5dp"
        android:layout_marginRight="5dp"
        android:layout_marginBottom="5dp"/>

    <TextView
        android:id="@+id/model_contactList_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/model_contactList_icon"
        android:textSize="19dp"
        android:text="@string/noset"
        android:textColor="@color/black"
        android:singleLine="true"/>

    <TextView
        android:id="@+id/model_contactList_description"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/model_contactList_icon"
        android:layout_below="@id/model_contactList_title"
        android:text="@string/noset"
        android:maxLines="3"
        android:ellipsize="end"
        android:textColor="@color/darkmiddlegrey" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:id="@+id/model_contactList_arrow"
        android:scaleType="center"
        android:minWidth="50dp"
        android:layout_alignTop="@+id/model_contactList_icon"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:src="@drawable/ic_action_arrow_right"
        android:paddingTop="10dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/noset"
        android:id="@+id/model_contactList_status"
        android:layout_alignTop="@+id/model_contactList_icon"
        android:layout_toRightOf="@+id/model_contactList_title"
        android:layout_toEndOf="@+id/model_contactList_title"
        android:layout_marginLeft="7dp"
        android:textSize="10sp"
        android:background="@drawable/new_message"
        android:textColor="@color/white"
        android:textIsSelectable="false"
        android:layout_marginTop="6dp"
        android:padding="3dp"
        android:visibility="gone"
        android:autoText="false"/>

</RelativeLayout>